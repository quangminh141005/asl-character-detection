# asl-character-detection
## Fast Guild
1. Create the conda env base on `enviroment_gpu.yml`(if gpu) and `enviroment_cpu`(if cpu)
2. run `./model-combine/pose/infer_hand_rtmpose_webcam.py` for webcam hand landmark and change `device` base on **cpu** or **gpu** ok?:D
Model for my ASL detection website project :Đ