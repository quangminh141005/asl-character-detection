# asl-character-detection
## Fast Guild
1. Create the conda env base on `enviroment_gpu.yml`(if gpu) and `enviroment_cpu`(if cpu)
2. run `./model-combine/pose/infer_hand_rtmpose_webcam.py` for webcam hand landmark and change `device` base on **cpu** or **gpu** 

Model for my [ASL detection website project](https://github.com/Tuan-Nguyen-Minhh/web-american-sign-language)